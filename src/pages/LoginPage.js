import LoginForm from '../components/Login/LoginForm';

// ----------------------------------------------------------------------------
const LoginPage = (props) => {
  // return (
  //   <div>
  //     <h2>Login</h2>
  //     <p>Add login page.</p>
  //   </div>
  // );
  return <LoginForm />;
};

export default LoginPage;
